{
  "name": "cs2-inventory-viewer",
  "version": "1.0.0",
  "private": true,
  "description": "React and Express app to view Steam Counter-Strike 2 inventories",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
